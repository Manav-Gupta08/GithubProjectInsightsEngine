# Core
flask==3.0.3
flask-cors==4.0.1
requests==2.32.3
python-dotenv==1.0.1
gunicorn==21.2.0

# Testing
pytest==8.2.2
pytest-cov==5.0.0

# ── Future additions ────────────
# redis==5.0.7                  # persistent caching (Phase 2)
# psycopg2-binary==2.9.9        # PostgreSQL driver (Phase 2)
# flask-sqlalchemy==3.1.1       # ORM (Phase 2)
# flask-migrate==4.0.7          # DB migrations (Phase 2)
# flask-jwt-extended==4.6.0     # JWT auth (Phase 3)
# anthropic==0.28.0             # AI insights (optional, already implemented via API)
